"""Synthetic test fixtures for bitwig-nitro-tools.

Everything here is generated from the public API, there is **no** Bitwig
content, no decrypted DSP, and no cipher keys anywhere in this package. The
one committed binary artifact (``synthetic_module.nitrobin``) is produced by
:func:`builders.build_synthetic_module` and can be regenerated at any time with
``python -m tests.fixtures.builders`` (see that module's ``__main__``).
"""
